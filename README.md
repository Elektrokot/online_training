# Онлайн-обучение (Online Training Platform)

## Описание

Проект представляет собой **платформу онлайн-обучения**, позволяющую пользователям:

- Регистрироваться и авторизовываться через JWT.
- Создавать, просматривать, редактировать и удалять курсы и уроки.
- Просматривать и оплачивать курсы и уроки.
- Просматривать историю платежей.
- Использовать фильтрацию и сортировку для поиска данных.
- Работать с группами пользователей: модераторы могут просматривать и редактировать любые курсы и уроки, но не могут их создавать или удалять.
- Обычные пользователи могут видеть, редактировать и удалять только свои курсы и уроки.
- Подписываться и отписываться от обновлений курсов.
- Валидация ссылок на видео: разрешены только ссылки на YouTube.
- Пагинация для списков курсов и уроков.
- Подключение оплаты через Stripe.
- Тестирование функционала с покрытием.
- **Асинхронная рассылка писем** при обновлении курса (только если курс не обновлялся более 4 часов).
- **Фоновая задача с `celery-beat`**, которая **блокирует пользователей**, не заходивших более месяца.

---

## 🌲 Структура проекта

```
online_training/
├── .venv/                     # Виртуальное окружение
├── config/                    # Основной модуль Django-проекта
│   ├── __init__.py
│   ├── asgi.py
│   ├── celery.py             # Файл настроек celery
│   ├── settings.py
│   ├── urls.py               # Основной URL-маршрутизатор
│   └── wsgi.py
├── courses/                   # Приложение для курсов и уроков
│   ├── management/            # Кастомные команды
│   │   └── commands/
│   │       └── create_moderator_group.py
│   ├── migrations/            # Миграции
│   ├── __init__.py
│   ├── services/              # Сервисы для работы с внешними API (Stripe)
│   │   └── strip_service.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py              # Модели Course, Lesson, Subscription
│   ├── permissions.py         # Права доступа
│   ├── serializers.py         # Сериализаторы
│   ├── tasks.py               # Задачи курса (рассылка писем приобновлении курса)
│   ├── validators.py          # Валидаторы (например, YouTubeLinkValidator)
│   ├── paginators.py          # Пагинаторы (для курсов и уроков)
│   ├── tests.py               # Тесты для курсов, уроков, подписок, валидаторов
│   ├── urls.py                # URL-маршруты
│   └── views.py               # Контроллеры
├── users/                     # Приложение для пользователей и платежей
│   ├── management/            # Кастомные команды
│   │   └── commands/
│   │       ├── create_db.py
│   │       └── load_payments.py
│   ├── migrations/            # Миграции
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py              # Модель User, Payment
│   ├── serializers.py         # Сериализаторы
│   ├── tasks.py               # Задачи пользователей (блокировка пользователей за неактивность)
│   ├── tests.py               # Тесты для пользователей и платежей
│   ├── urls.py                # URL-маршруты
│   └── views.py               # Контроллеры
├── .env                       # Переменные окружения
├── .env.sample                # Пример переменных окружения
├── .flake8                    # Настройки flake8
├── .gitignore                 # Исключения для git
├── manage.py
├── poetry.lock
├── pyproject.toml             # Зависимости и настройки Poetry
└── README.md                  # Этот файл
```

---

## 🛠️ Установка

1. Клонируйте репозиторий:

   ```bash
   git clone https://github.com/Elektrokot/online_training.git
   cd online_training
   ```

2. Установите зависимости:

   ```bash
   poetry install
   ```

3. Создайте базу данных (используются настройки из `.env`):

   ```bash
   python manage.py create_db
   ```
   
4. Выполните миграции:

   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

5. Заполнение базы тестовыми данными:

   ```bash
   python manage.py load_payments
   ```

6. Создайте группу модераторов:

   ```bash
   python manage.py create_moderator_group
   ```

7. Создайте суперпользователя:

   ```bash
   python manage.py createsuperuser
   ```

8. Запустите сервер:

   ```bash
   python manage.py runserver
   ```
   
### 🐳 Запуск проекта через Docker Compose
Для запуска проекта используется docker-compose. Все сервисы (Django, PostgreSQL, Redis, Celery, Celery Beat) запускаются в контейнерах.

#### 📋 Предварительные требования
- Установленный Docker

#### 🚀 Команда для запуска проекта
```bash
docker-compose up -d --build
```
Флаг --build пересобирает образы перед запуском, если были изменения в Dockerfile.

#### 🧪 Проверка работоспособности сервисов
После запуска вы можете проверить работоспособность следующих сервисов:

- **Django API**:
  - Откройте в браузере http://localhost:8000
  - API документация будет доступна по адресу http://localhost:8000/api/docs/#/

- **PostgreSQL**:
  - Подключение осуществляется внутри контейнера по адресу db:5432.
  - Логи можно посмотреть через:
    ```bash
    docker-compose logs db
    ```

- **Redis**:
  - Используется как брокер для Celery.
  - Логи Redis:
    ```bash
    docker-compose logs redis
    ```

- **Celery Worker**:
  - Обрабатывает асинхронные задачи (например, отправка уведомлений).
  - Логи:
    ```bash
    docker-compose logs celery_worker
    ```

- **Celery Beat**:
  - Запускает периодические задачи (например, деактивация пользователей).
  - Логи:
    ```bash
    docker-compose logs celery_beat
    ```

- **Админка Django**:
  - Доступна по адресу http://localhost:8000/admin/
  - Убедитесь, что суперпользователь создан (см. ниже).

#### 🛠️ Дополнительные команды
- Создать суперпользователя:
  ```bash
  docker-compose exec web python manage.py createsuperuser
  ```

- Выполнить миграции (если не выполнены автоматически):
  ```bash
  docker-compose exec web python manage.py migrate
  ```

- Остановить все контейнеры:
  ```bash
  docker-compose down
  ```

- Остановить и удалить все контейнеры, сети и тома:
  ```bash
  docker-compose down -v
  ```

#### ✅ Успешный запуск
Если всё настроено корректно, вы должны увидеть:
- Админка доступна по http://localhost:8000/admin/
- Celery и Beat запущены и обрабатывают задачи
- База данных PostgreSQL и Redis работают

## 🔄 Запуск Celery и Celery-Beat

Для работы асинхронных задач и периодических задач:

1. Запустите воркер Celery:

   ```bash
   celery -A config worker --loglevel=info --pool=solo
   ```

2. Запустите планировщик Celery-Beat:

   ```bash
   celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
   ```
   
---

## 🔐 API Endpoints

### Пользователи

- `GET /users/` – получить список пользователей
- `POST /login/` – вход для зарегистрированных пользователей
- `POST /register/` – создать пользователя
- `GET /users/{id}/` – получить информацию о пользователе
- `PUT /users/{id}/` – обновить пользователя
- `PATCH /users/{id}/` – частично обновить пользователя
- `DELETE /users/delete/{id}/` – удалить пользователя (только администратор имеет право)

### Курсы

- `GET /courses/` – получить список курсов
- `POST /courses/` – создать курс
- `GET /courses/{id}/` – получить курс
- `PUT /courses/{id}/` – обновить курс
- `PATCH /courses/{id}/` – частично обновить курс
- `DELETE /courses/{id}/` – удалить курс

### Уроки

- `GET /lessons/` – получить список уроков
- `POST /lessons/create/` – создать урок
- `GET /lessons/{id}/` – получить урок
- `PUT /lessons/update/{id}/` – обновить урок
- `DELETE /lessons/delete/{id}/` – удалить урок

### Платежи

- `GET /payments/` – получить список платежей
- `POST /payments/create/` – создать платеж и получить ссылку на оплату через Stripe
- `GET /payments/status/{session_id}/` – получить статус платежа по ID сессии Stripe
- Параметры фильтрации:
  - `paid_course` – фильтр по курсу
  - `paid_lesson` – фильтр по уроку
  - `payment_method` – фильтр по способу оплаты
- Параметры сортировки:
  - `ordering=payment_date` – по возрастанию даты
  - `ordering=-payment_date` – по убыванию даты

### Подписки

- `POST courses/<int:course_id>/subscription/subscription/` – подписаться или отписаться от курса

---

## 🧪 Примеры запросов в Postman

### Регистрация пользователя

- Method: `POST`
- URL: `http://127.0.0.1:8000/register/`
- Body (JSON):

```json
{
  "email": "test@example.com",
  "password": "test123",
  "phone": "+79999999999",
  "city": "Москва"
}
```

### Вход зарегистрированного пользователя

- Method: `POST`
- URL: `http://127.0.0.1:8000/login/`
- Body (JSON):

```json
{
  "email": "test@example.com",
  "password": "test123"
}
```

### Получение списка курсов

- Method: `GET`
- URL: `http://127.0.0.1:8000/courses/`

### Создание урока

- Method: `POST`
- URL: `http://127.0.0.1:8000/lessons/`
- Body (JSON):

```json
{
  "title": "Variables",
  "description": "Introduction to variables",
  "video_url": "https://youtube.com/watch?v=...",
  "course": 1,
  "owner": 1
}
```

### Фильтрация платежей

- Method: `GET`
- URL: `http://127.0.0.1:8000/payments/?paid_course=1&ordering=-payment_date`

### Создание платежа и получение ссылки на оплату

- Method: `POST`
- URL: `http://127.0.0.1:8000/payments/create/`
- Headers: `Authorization: Bearer <token>`
- Body (JSON):

```json
{
  "paid_course": 1,
  "paid_lesson": 1,
  "amount": "1000",
  "payment_method": "transfer"
}
```

- Ответ:

```json
{
  "session_url": "https://checkout.stripe.com/c/pay/cs_test_..."
}
```

### Проверка статуса платежа

- Method: `GET`
- URL: `http://127.0.0.1:8000/payments/status/cs_test_a160i7BrbhkklpR3VarKnq1PGYTRlsmJP5SE6ZmyeCOY7yNKmxqbpKfvHI/`
- Headers: `Authorization: Bearer <token>`

- Ответ:

```json
{
  "status": "paid"
}
```

### Управление подпиской

- Method: `POST`
- URL: `http://127.0.0.1:8000/courses/<int:course_id>/subscription/`
- Headers: `Authorization: Bearer <token>`
- Body (JSON):

```json
{
  "course_id": 1
}
```
---

## ⚙️ Требования к окружению

- Python 3.9+
- PostgreSQL
- Django 5.0+
- DRF + SimpleJWT
- Celery + Redis + django-celery-beat

---

## 📝 Известные ограничения

- В разработке.

---

## 🚀 Планы по доработке

- В разработке

---

## 👤 Автор

Константин Чирков  
[GitHub](https://github.com/Elektrokot)

---

## 📜 Лицензия

MIT License